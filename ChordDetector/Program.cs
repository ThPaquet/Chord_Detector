using System;
using System.Linq;
using static ChordDetector.Keyboard;
using static ChordDetector.Fonctions;
using System.Collections.Generic;

namespace ChordDetector
{
    class Program
    {
        static void Main(string[] args)
        {
            /*string testNote = "C";
            string testNote2 = "E";

            List<string> interval = new List<string>();

            interval.Add(testNote);
            interval.Add(testNote2);

            Keyboard keyboard = new Keyboard();

            Console.Out.WriteLine(CalculateIntervalNotes(interval));*/
        }

        
    }
}
